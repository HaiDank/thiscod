export default defineAppConfig({
    ui: {
        colors: {
            primary: "indigo",
        },
        tooltip: {
            slots: {
                text: "text-base",
                content: " bg-selected h-auto",
            },

        },
    },
});
